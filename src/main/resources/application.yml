spring:
  application:
    name: url-shortener
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/url_shortener}
    username: ${DATABASE_USERNAME:url_shortener}
    password: ${DATABASE_PASSWORD:url_shortener}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
